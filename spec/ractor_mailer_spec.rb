# frozen_string_literal: true

RSpec.describe RactorMailer do
  it "has a version number" do
    expect(RactorMailer::VERSION).not_to be_nil
  end

  it "does something useful" do
    expect(false).not_to be_truthy
  end
end
